{
  "name": "alalqami-agent",
  "version": "0.1.1",
  "private": true,
  "workspaces": ["packages/*", "services/*"],
  "scripts": {
    "dev:api": "npm --workspace @alalqami/api run dev",
    "check": "npm run check --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
